<div class="contact-title">Telefones</div>
{% for phone in phones %}
<a class="contact-number" href="tel:{{ phone.number | replace('-', '') }}">{{ phone.number }}</a>
{% if phone.oper %} 
<div class="contact-subtitle">({{ phone.oper }})</div>
{% endif %}
{% endfor %}
